package main

import "fmt"

func maidn() {

	// var initArray [3]int
	// initArray[0] = 1
	// var arr = [3][3]int{{0, 1, 2}, {0, 1, 2}, {0, 1, 2}}
	// var arr2 = [3]string{"a", "b", "c"}
	// var slice = []int{}
	// for j, val := range arr2 {
	// 	fmt.Printf("%d==>%s\n", j, val)
	// }
	// for i := 0; i < len(arr); i++ {
	// 	fmt.Printf("%d\n", arr[i])
	// }
	// arr[0] = arr[1]
	// fmt.Printf("%d\n", arr[0])
	// slice = append(slice, 1)
	// slice = append(slice, 2)
	// slice = append(slice, 3)
	// for i, val := range slice {
	// 	fmt.Printf("%d==>%d\n", i, val)
	// }
	// // initArray = arr[0:1]
	// newSlice := make([]string, 3)
	// fmt.Printf("%q", newSlice[0:0])
	// slice = append(slice[:1], slice[1+1:]...)
	// for i, val := range slice {
	// 	fmt.Printf("%d==>%d\n", i, val)
	// }

	// maps
	// myFirsMap := map[int]int{0: 1, 1: 2}
	// newMap := map[string]string{"one": "one", "tow": "tow"}
	// for key, value := range myFirsMap {
	// 	fmt.Printf("the index is %d and the value is ==>%d\n", key, value)
	// }

	// newMap["three"] = "three"
	// for i, val := range newMap {
	// 	fmt.Printf("the index is %s and the value is ==>%s\n", i, val)
	// }
	// delete(newMap, "three")
	// for i, val := range newMap {
	// 	fmt.Printf("the index is %s and the value is ==>%s\n", i, val)
	// }
}
